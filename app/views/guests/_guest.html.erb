<div id="guest-<%= guest.id %>" class="guest panel panel-default">
  <div class="panel-body">
    <p>
    <h3 <%= 'id=current_user' if current_guest?(guest) %>><div class="status-dot <%= guest.status %>"></div><%= guest.name %></h3>
    </p>
  </div>
</div>

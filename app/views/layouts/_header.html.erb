<div class="header">
  <div class="navbar navbar-default">
    <div class="container-fluid">
      <div class="navbar-header">
				<button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-menu">
					<span class="icon-bar"></span>
					<span class="icon-bar"></span>
					<span class="icon-bar"></span> 
				</button>
        <img src="/images/Wellen-lineart-800px.png" alt="WaveCastr" height="80">
        <a class="h1" href="/">WaveCastr</a>
      </div>
      <div class="collapse navbar-collapse" id="navbar-menu">
        <ul class="nav navbar-right nav-pills">
          <% if user_signed_in? %>
            <li><%= link_to current_user.name, user_path(current_user), class: 'nav-btn text-gold' %></li>
            <li>
              <%= link_to('Logout', destroy_user_session_path, :method => :delete, class: 'nav-btn text-gold') %>
            </li>
          <% else %>
            <li>
              <%= link_to('Register', new_user_registration_path, class: 'text-gold') %>
            </li>
            <li>
            <%= link_to('Login', new_user_session_path, class: 'text-gold') %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</div>

<!DOCTYPE html>
<html>
  <head>
    <title>Wavecastr</title>
    <link href="https://fonts.googleapis.com/css?family=Anton|Indie+Flower|Supermercado+One|Titillium+Web" rel="stylesheet">
    <script src="https://use.fontawesome.com/759e55b199.js"></script>
    <%= csrf_meta_tags %>
    <%= action_cable_meta_tag %>
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>

  </head>

  <body>
    <div class="header">
      <div class="navbar navbar-default">
        <div class="container-fluid">
          <div class="navbar-header">
            <img src="/Wellen-lineart-800px.png" alt="WaveCastr" height="80">
            <a class="h1" href="/">WaveCastr</a>
          </div>
        </div>
      </div>
    </div>
    <div class="container">
      <div id="flash">
        <% flash.each do |name, msg| -%>
          <%= content_tag :div, msg, class: name unless name == "notice" %>
        <% end %>
      </div>
      <%= render(template: 'episodes/show', layout: false) %>
    </div>
  </body>
</html>
